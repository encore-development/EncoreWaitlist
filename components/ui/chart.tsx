return (
    <div
      className={cn(
        'bg-background grid min-w-[8rem] items-start gap-1.5 rounded-lg border border-slate-200/50 px-2.5 py-1.5 text-xs shadow-xl',
        className,
      )}
    >
      {/* rest of code here */}
    </div>
  );
